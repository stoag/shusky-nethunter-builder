name: Build Pixel 8 Pro (shusky) - NetHunter + KernelSU Next + ConfigFS

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    env:
      CUSTOM_KERNEL_VERSION: "twk.v1.00"
      
    steps:
    - name: Free Runner Disk Space
      run: |
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf "/usr/local/share/boost"
        sudo apt-get clean
        df -h

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc build-essential curl flex bison libssl-dev \
          libelf-dev python3 repo git zip libncurses5-dev

    - name: Configure Git Credentials
      run: |
        git config --global user.name "Builder"
        git config --global user.email "builder@localhost"

    - name: Cache Bazel & Kleaf Build Output
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/bazel
          workspace/out/bazel
        key: ${{ runner.os }}-bazel-shusky-${{ hashFiles('workspace/private/devices/google/shusky/**') }}
        restore-keys: |
          ${{ runner.os }}-bazel-shusky-

    - name: Initialize & Sync Google Kernel Source
      run: |
        mkdir workspace && cd workspace
        # Target the official Android 16 / Tensor G3 (shusky) branch
        repo init -u https://android.googlesource.com/kernel/manifest \
            -b android-gs-shusky-6.1-android16 --depth=1
        
        # Sync required repositories to conserve runner storage
        repo sync -c -j$(nproc) --no-tags --no-clone-bundle

    - name: Apply Kali NetHunter Drivers & Patches
      run: |
        cd workspace/aosp # Main GKI Linux kernel tree
        
        # 1. Download and apply Kali mac80211 Wi-Fi injection patch safely
        curl -L -o injection.patch https://gitlab.com/kalilinux/nethunter/build-scripts/kali-nethunter-project/-/raw/master/patches/6.1/0001-mac80211-injection.patch
        
        # Verify it's a real patch file and not an HTML error page
        if grep -q "<html" injection.patch; then
          echo "ERROR: Downloaded file is HTML, not a patch! Check URL."
          exit 1
        fi
        
        patch -p1 < injection.patch || true
        rm injection.patch

        # 2. Mark MAC80211_LEDS as broken in Kconfig to prevent missing LED symbol panics
        find drivers/net/wireless -name "Kconfig" -exec sed -i '/select MAC80211_LEDS/d' {} +
        sed -i 's/config MAC80211_LEDS/config MAC80211_LEDS\n\tdepends on BROKEN/g' net/mac80211/Kconfig

        # 3. Download and apply HID gadget patch (USB Arsenal / DuckHunter) safely
        curl -L -o hid.patch https://gitlab.com/kalilinux/nethunter/build-scripts/kali-nethunter-project/-/raw/master/patches/6.1/0002-hid-gadget.patch
        
        if grep -q "<html" hid.patch; then
          echo "ERROR: Downloaded file is HTML, not a patch! Check URL."
          exit 1
        fi
        
        patch -p1 < hid.patch || true
        rm hid.patch

    - name: Apply KernelSU Next Patches & Configs
      run: |
        cd workspace/aosp
        
        # 1. Inject KernelSU Next inline source code
        curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -
        
        # 2. Dynamically inject your version variable into the config
        echo "CONFIG_LOCALVERSION=\"${{ env.CUSTOM_KERNEL_VERSION }}\"" >> arch/arm64/configs/gki_defconfig
        
        # 3. Append the rest of your configs safely using a quoted EOF block
        cat << 'EOF' >> arch/arm64/configs/gki_defconfig
        # Inline KernelSU Next
        CONFIG_KSU=y
        # Disable Google GKI Symbol Trimming
        CONFIG_TRIM_NONLISTED_KMI=n
        # USB ConfigFS & HID Arsenal
        CONFIG_CONFIGFS_FS=y
        CONFIG_USB_CONFIGFS=y
        CONFIG_USB_GADGETFS=y
        CONFIG_USB_CONFIGFS_F_HID=y
        CONFIG_USB_CONFIGFS_F_MASS_STORAGE=y
        CONFIG_USB_CONFIGFS_F_ECM=y
        CONFIG_USB_CONFIGFS_F_ACM=y
        # Mac80211 Subsystem (Wireless Injection Support)
        CONFIG_MAC80211=y
        CONFIG_MAC80211_RC_MINSTREL=y
        CONFIG_MAC80211_RC_MINSTREL_HT=y
        CONFIG_MAC80211_RC_MINSTREL_VHT=y
        CONFIG_MAC80211_RC_DEFAULT_MINSTREL=y
        CONFIG_MAC80211_MESH=y
        CONFIG_CFG80211=y
        # CONFIG_MAC80211_LEDS is not set
        
        # External Wi-Fi Adapters (Atheros, Realtek, & Ralink)
        CONFIG_WLAN_VENDOR_ATH=y
        CONFIG_ATH9K_HTC=m
        CONFIG_WLAN_VENDOR_REALTEK=y
        CONFIG_RTL8187=m
        CONFIG_RTL8XXXU=m
        CONFIG_WLAN_VENDOR_RALINK=y
        CONFIG_RT2X00=m
        CONFIG_RT2X00_USB=m
        CONFIG_RT2800_LIB=m
        CONFIG_RT2800_USB=m
        
        # Bluetooth & VPN Support
        CONFIG_BT_HCIBTUSB=m
        CONFIG_WIREGUARD=y
        
        # CPU Governors
        CONFIG_CPU_FREQ_GOV_PERFORMANCE=y
        CONFIG_CPU_FREQ_GOV_POWERSAVE=y
        CONFIG_CPU_FREQ_GOV_USERSPACE=y
        CONFIG_CPU_FREQ_GOV_ONDEMAND=y
        CONFIG_CPU_FREQ_GOV_CONSERVATIVE=y
        # Thermal & Power Debugging
        CONFIG_POWER_SUPPLY_DEBUG=y
        CONFIG_DEBUG_FS=y
        CONFIG_THERMAL_GOV_USER_SPACE=y
        # Networking Optimizations & BBR TCP
        CONFIG_TCP_CONG_BBR=y
        CONFIG_NET_SCH_FQ=y
        CONFIG_DEFAULT_BBR=y
        # TTL Modification Support
        CONFIG_NETFILTER_XT_TARGET_HL=y
        CONFIG_IP_NF_TARGET_TTL=y
        CONFIG_IP6_NF_TARGET_HL=y
        # Drive & Filesystem Support
        CONFIG_NTFS3_FS=y
        CONFIG_EXFAT_FS=y
        CONFIG_ISO9660_FS=y
        CONFIG_UDF_FS=y
        EOF

    - name: Authorize NetHunter Symbols in GKI
      run: |
        cd workspace
        
        # 1. Master list of required Kali network & driver symbols
        cat << 'EOF' > missing_symbols.txt
        __dev_kfree_skb_irq
        __hci_cmd_sync
        __hci_cmd_sync_ev
        __printk_ratelimit
        __regmap_init
        bit_wait_timeout
        bt_
